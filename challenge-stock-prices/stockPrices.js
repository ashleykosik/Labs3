
let a = [1, 2, 3, 4, 5]
let b = [100, 50, 20]
let c = [5, 2, 7, 9]

function best(num) {
    let min =[]   
    
    //is the first number not the max / is this the max
   for(i = 1; i < num.length; i++)  {
    if (num[0] > num[i]) {
       return console.log('$0')
    } else if (num[0] < num[i]) {
        return num[0] - num[i]
    }
    
    //find min
}
}

// if the first number is lower